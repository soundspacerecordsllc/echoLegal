interface TrustStripProps {
  lang: 'en' | 'tr'
}

export default function TrustStrip({ lang }: TrustStripProps) {
  // Hidden — credentials are conveyed in the About section and footer instead
  return null
}
